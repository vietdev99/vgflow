# R3.5 Roam Pilot Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Refactor `commands/vg/roam.md` from 1,103 → ≤500-line slim entry + ~7 flat refs in `commands/vg/_shared/roam/` (1 nested dir: `config-gate/` for the decomposed mega-gate). **No new subagents** — decomposition only. Decompose step `0a_env_model_mode_gate` (355 lines, 9 sub-prompts inline) into 5 sub-steps. Strengthen `roam.native_tasklist_projected` emission (PARTIAL — 5 events declared but projection not in current set). Bake all 3 R1a UX baseline requirements + vg-load consumption (Phase F Task 30 absorbed for vg:roam — minimal: only L600 read).

**Architecture:** Reuse 100% of R1a infrastructure. Roam-specific work: 0 new subagents (existing auto-fix loop subagent at step 7 preserved as-is) + 1 mega-gate decomposition + 1 slim entry + ~7 refs. Other patterns preserved as-is: lens auto-pick by phase profile + entity types, per-surface composition (Cartesian), 3 dispatch branches (spawn/self/manual), evidence-based state coherence (R1-R8 deterministic Python rules), vocabulary validator, staged spec merge (`--merge-specs`), auto-fix loop (max 5 fixes/session).

**Tech Stack:** bash, Python 3 (tests), pytest, Claude Code Agent tool, sqlite3, HMAC-SHA256 (signed evidence — reused).

**Spec source:** `docs/superpowers/specs/2026-05-03-vg-roam-design.md` (278 lines, includes Codex review corrections + UX baseline).

**Branch:** `feat/rfc-v9-followup-fixes`. Each task commits incrementally. Final dogfood on a PrintwayV3 phase where review + test both PASSED.

**Phase F Task 30 absorption:** vg:roam portion absorbed via Task 5 (discovery.md uses vg-load --index for surface enumeration). Roam's flat-read footprint is minimal (single L600 in original — `Read PLAN.md + CONTEXT.md + RUNTIME-MAP.md` for CRUD surface identification). After R3.5 verdict (Task 12), update blueprint plan Phase F Task 30 to remove `vg:roam` from scope.

**Sequencing:** R3.5 (between R3 review and R4 accept). Roam shares lens-prompts/ with review — must wait for R3 to stabilize lens architecture changes (per-lens telemetry, staleness check) before applying same patterns to roam.

---

## File structure (new + modified)

| File | Action | Lines | Purpose |
|---|---|---|---|
| `commands/vg/roam.md` | REFACTOR (1103 → ~500) | -603 | Slim entry per blueprint pilot template |
| `commands/vg/.roam.md.r3.5-backup` | CREATE | 1103 | Backup |
| `commands/vg/_shared/roam/preflight.md` | CREATE | ~150 | 0_parse_and_validate + 0aa_resume_check |
| `commands/vg/_shared/roam/config-gate/overview.md` | CREATE | ~100 | Entry, 5-sub-step sequence narrative |
| `commands/vg/_shared/roam/config-gate/backfill-env.md` | CREATE | ~100 | 0a_backfill_env_pref (pre-prompt 1, 90 lines) |
| `commands/vg/_shared/roam/config-gate/detect-platform.md` | CREATE | ~100 | 0a_detect_platform_tools (pre-prompt 1.5, 73 lines) |
| `commands/vg/_shared/roam/config-gate/enrich-env.md` | CREATE | ~100 | 0a_enrich_env_options (pre-prompt 2, 32 lines) |
| `commands/vg/_shared/roam/config-gate/confirm-env-model-mode.md` | CREATE | ~100 | 0a_confirm_env_model_mode (AskUserQuestion 3-question, 42 lines) |
| `commands/vg/_shared/roam/config-gate/persist-config.md` | CREATE | ~100 | 0a_persist_config (after-answers branch, 91 lines) |
| `commands/vg/_shared/roam/discovery.md` | CREATE | ~150 | 1_discover_surfaces + 2_compose_briefs |
| `commands/vg/_shared/roam/spawn-executors.md` | CREATE | ~250 | 3_spawn_executors with 3 branches (spawn/self/manual) |
| `commands/vg/_shared/roam/aggregate-analyze.md` | CREATE | ~150 | 4_aggregate_logs + 5_analyze_findings |
| `commands/vg/_shared/roam/artifacts.md` | CREATE | ~150 | 6_emit_artifacts + spec.ts proposal generation |
| `commands/vg/_shared/roam/fix-loop.md` | CREATE | ~100 | 7_optional_fix_loop (existing subagent preserved) |
| `commands/vg/_shared/roam/close.md` | CREATE | ~100 | complete + merge-specs invocation note |
| `scripts/emit-tasklist.py` | MODIFY | +0 | Add CHECKLIST_DEFS["vg:roam"] |
| `scripts/tests/test_roam_slim_size.py` | CREATE | ~50 | Assert roam.md ≤600 lines, refs listed |
| `scripts/tests/test_roam_references_exist.py` | CREATE | ~60 | All 7 refs + 5 config-gate sub-refs |
| `scripts/tests/test_roam_step_0a_decomposed.py` | CREATE | ~80 | Assert 5 sub-step markers replace single 0a |
| `scripts/tests/test_roam_no_new_subagents.py` | CREATE | ~50 | Assert no new agent SKILL.md (auto-fix preserved) |
| `scripts/tests/test_roam_uses_vg_load.py` | CREATE | ~80 | Discovery uses vg-load --index for surfaces |

**Total: 1 modified + 18 created. ~1500 lines added, ~603 lines removed.**

---

## Phase A — Backup

### Task 1: Backup current roam.md

- [ ] **Step 1: Backup**

```bash
cd "/Users/dzungnguyen/Vibe Code/Code/vgflow-bugfix"
cp commands/vg/roam.md commands/vg/.roam.md.r3.5-backup
wc -l commands/vg/.roam.md.r3.5-backup   # Expected: 1103
```

- [ ] **Step 2: Commit**

```bash
git add commands/vg/.roam.md.r3.5-backup
git commit -m "chore(r3.5-roam): backup roam.md before slim refactor (1103 lines)"
```

---

## Phase B — Roam slim refs (7 files + 5-way mega-gate decomposition)

### Task 2: Create _shared/roam/preflight.md

**Files:**
- Create: `commands/vg/_shared/roam/preflight.md` (~150 lines)

- [ ] **Step 1: Extract 0_parse_and_validate + 0aa_resume_check**

```markdown
# Roam preflight — STEP 1

<HARD-GATE>
Roam is post-confirmation (runs AFTER /vg:review + /vg:test PASS).
You MUST verify both passes BEFORE any roam step.
TodoWrite IMPERATIVE after emit-tasklist.py.
</HARD-GATE>
[Sub-steps from backup]
```

- [ ] **Step 2: Commit**

```bash
git add commands/vg/_shared/roam/preflight.md
git commit -m "feat(r3.5-roam): preflight ref — parse + resume check"
```

---

### Task 3: Create _shared/roam/config-gate/ (6 files: overview + 5 sub-steps)

**Files:**
- Create: `commands/vg/_shared/roam/config-gate/overview.md` (~100 lines)
- Create: `commands/vg/_shared/roam/config-gate/backfill-env.md` (~100 lines)
- Create: `commands/vg/_shared/roam/config-gate/detect-platform.md` (~100 lines)
- Create: `commands/vg/_shared/roam/config-gate/enrich-env.md` (~100 lines)
- Create: `commands/vg/_shared/roam/config-gate/confirm-env-model-mode.md` (~100 lines)
- Create: `commands/vg/_shared/roam/config-gate/persist-config.md` (~100 lines)

- [ ] **Step 1: Decompose 0a_env_model_mode_gate (355 lines, 9 sub-prompts inline) into 5 sub-steps**

Per spec §1.2: the mega-gate contains:
1. Pre-prompt 1 (env preference backfill, 90 lines) → `backfill-env.md`
2. Pre-prompt 1.5 (platform + tool detection, 73 lines) → `detect-platform.md`
3. Pre-prompt 2 (enrich env from DEPLOY-STATE, 32 lines) → `enrich-env.md`
4. AskUserQuestion 3-question batch (env + model + mode, 42 lines) → `confirm-env-model-mode.md`
5. After-answers branch (resolve + validate + persist, 91 lines) → `persist-config.md`

- [ ] **Step 2: Write overview.md (sequence narrative)**

```markdown
# Config gate — STEP 2 (decomposed mega-gate)

<HARD-GATE>
Original 0a_env_model_mode_gate was 355 lines with 9 sub-prompts inline.
Decomposed into 5 sequential sub-steps. Each sub-step has its own marker.
Anti-forge guard refuses launch if all 3 axes (env/model/mode) empty.
</HARD-GATE>

## Sequence

1. Read `backfill-env.md` (0a_backfill_env_pref)
2. Read `detect-platform.md` (0a_detect_platform_tools)
3. Read `enrich-env.md` (0a_enrich_env_options)
4. Read `confirm-env-model-mode.md` (0a_confirm_env_model_mode)
5. Read `persist-config.md` (0a_persist_config)

Each sub-step emits its own step-active + step-mark events.
```

- [ ] **Step 3: Write 5 sub-step refs**

Each ref ≤150 lines, single-purpose. Markers:
- `0a_backfill_env_pref`
- `0a_detect_platform_tools`
- `0a_enrich_env_options`
- `0a_confirm_env_model_mode`
- `0a_persist_config`

- [ ] **Step 4: Commit**

```bash
git add commands/vg/_shared/roam/config-gate/
git commit -m "feat(r3.5-roam): config-gate decomposition — 1 mega-gate → 5 sub-steps

0a_env_model_mode_gate (355 lines, 9 sub-prompts inline) decomposed into
5 sub-step refs each ≤150 lines. Each sub-step has its own marker
(audit FAIL #10 fix). Interactive UX preserved — AskUserQuestion stays
in main agent (config-gate is not subagent extraction)."
```

---

### Task 4: Create _shared/roam/discovery.md

**Files:**
- Create: `commands/vg/_shared/roam/discovery.md` (~150 lines)

- [ ] **Step 1: Extract 1_discover_surfaces + 2_compose_briefs**

vg-load consumption: surface discovery uses `vg-load --phase N --artifact plan --index` for task list (NOT cat PLAN.md). Closes Phase F Task 30 for vg:roam (single L600 reference in backup).

- [ ] **Step 2: Write ref**

```markdown
# Discovery + briefs — STEP 3

## Surface discovery

Use `vg-load --phase ${PHASE_NUMBER} --artifact plan --index` to get task list,
then per-task expand only when CRUD surface drill-down needed.

Read CONTEXT.md (KEEP-FLAT — small single doc).
Read RUNTIME-MAP.md (KEEP-FLAT — JSON, already filtered by /vg:review).

DO NOT cat ${PHASE_DIR}/PLAN.md (8K+ lines on large phases — AI will skim).

## Brief composition

Per surface × lens (Cartesian): write INSTRUCTION-{surface}-{lens}.md to
${PHASE_DIR}/roam/briefs/. Lens auto-pick by phase profile + entity types.
```

- [ ] **Step 3: Commit**

```bash
git add commands/vg/_shared/roam/discovery.md
git commit -m "feat(r3.5-roam): discovery ref — vg-load --index for surfaces

Closes Phase F Task 30 for vg:roam (single L600 PLAN.md flat read replaced
by vg-load --index)."
```

---

### Task 5: Create _shared/roam/spawn-executors.md

**Files:**
- Create: `commands/vg/_shared/roam/spawn-executors.md` (~250 lines)

- [ ] **Step 1: Extract 3_spawn_executors (200 lines) with 3 branches**

- Branch A: spawn subprocess (throttle 5 parallel)
- Branch B: self via Playwright MCP
- Branch C: manual paste-prompt

Each branch preserves narrate-spawn for sub-agent / sub-process invocations.

- [ ] **Step 2: Commit**

```bash
git add commands/vg/_shared/roam/spawn-executors.md
git commit -m "feat(r3.5-roam): spawn-executors ref — 3 dispatch branches preserved"
```

---

### Task 6: Create _shared/roam/aggregate-analyze.md

**Files:**
- Create: `commands/vg/_shared/roam/aggregate-analyze.md` (~150 lines)

- [ ] **Step 1: Extract 4_aggregate_logs + 5_analyze_findings**

State coherence assertion via R1-R8 deterministic Python rules in `roam-analyze.py` (preserved as-is).

Vocabulary validator preserved (banned tokens tagged `vocabulary_violation: true`).

- [ ] **Step 2: Commit**

```bash
git add commands/vg/_shared/roam/aggregate-analyze.md
git commit -m "feat(r3.5-roam): aggregate-analyze ref — R1-R8 + vocabulary validator preserved"
```

---

### Task 7: Create _shared/roam/artifacts.md

**Files:**
- Create: `commands/vg/_shared/roam/artifacts.md` (~150 lines)

- [ ] **Step 1: Extract 6_emit_artifacts + spec.ts proposal generation**

Spec.ts proposals stage to `proposed-specs/` (NOT auto-merged — requires `--merge-specs` flag).

- [ ] **Step 2: Commit**

```bash
git add commands/vg/_shared/roam/artifacts.md
git commit -m "feat(r3.5-roam): artifacts ref — staged spec.ts proposal generation"
```

---

### Task 8: Create _shared/roam/fix-loop.md

**Files:**
- Create: `commands/vg/_shared/roam/fix-loop.md` (~100 lines)

- [ ] **Step 1: Extract 7_optional_fix_loop**

Existing auto-fix loop subagent (max 5 fixes/session) preserved as-is. This ref documents the spawn site with narrate-spawn calls.

- [ ] **Step 2: Commit**

```bash
git add commands/vg/_shared/roam/fix-loop.md
git commit -m "feat(r3.5-roam): fix-loop ref — existing auto-fix subagent preserved"
```

---

### Task 9: Create _shared/roam/close.md

**Files:**
- Create: `commands/vg/_shared/roam/close.md` (~100 lines)

- [ ] **Step 1: Extract complete + merge-specs invocation note**

- [ ] **Step 2: Commit**

```bash
git add commands/vg/_shared/roam/close.md
git commit -m "feat(r3.5-roam): close ref — complete + --merge-specs note"
```

---

## Phase C — Slim entry replacement

### Task 10: Replace roam.md body with slim entry

**Files:**
- Modify: `commands/vg/roam.md` (1103 → ~500 lines)

- [ ] **Step 1: Build slim entry**

```yaml
---
name: vg:roam
description: Exploratory CRUD-lifecycle pass with state-coherence assertion (post-review/test janitor)
argument-hint: "<phase> [--auto-fix] [--merge-specs] [--non-interactive] [--lens=<name>]"
allowed-tools: [Read, Write, Edit, Bash, Glob, Grep, AskUserQuestion, Agent, TodoWrite]
runtime_contract:
  must_write:
    - "${PHASE_DIR}/roam/SURFACES.md"
    - "${PHASE_DIR}/roam/RAW-LOG.jsonl"
    - "${PHASE_DIR}/roam/ROAM-BUGS.md"
    - "${PHASE_DIR}/roam/RUN-SUMMARY.json"
  must_touch_markers:
    - "0_parse_and_validate"
    # Decomposed mega-gate (5 sub-steps replace single 0a)
    - "0a_backfill_env_pref"
    - "0a_detect_platform_tools"
    - "0a_enrich_env_options"
    - "0a_confirm_env_model_mode"
    - "0a_persist_config"
    - "0aa_resume_check"
    - "1_discover_surfaces"
    - "2_compose_briefs"
    - "3_spawn_executors"
    - "4_aggregate_logs"
    - "5_analyze_findings"
    - "6_emit_artifacts"
    - "complete"
    - name: "7_optional_fix_loop"
      severity: "warn"
      required_unless_flag: "--auto-fix"
  must_emit_telemetry:
    - "roam.tasklist_shown"
    - "roam.native_tasklist_projected"   # PARTIAL audit fix
    - "roam.session.started"
    - "roam.session.completed"
    - "roam.analysis.completed"
    - event_type: "roam.resume_mode_chosen"
      required_unless_flag: "--non-interactive"
    - event_type: "roam.config_confirmed"
      required_unless_flag: "--non-interactive"
  forbidden_without_override:
    - "--non-interactive"
    - "--skip-pre-check"
    - "--override-reason"
---

<HARD-GATE>
Roam is post-confirmation (runs AFTER /vg:review + /vg:test PASS).
You MUST verify both passes BEFORE any roam step.
Lens auto-pick by phase profile + entity types — DO NOT manually override
unless --lens flag explicit. Spec.ts proposals stage to proposed-specs/ —
DO NOT auto-merge (requires --merge-specs).
</HARD-GATE>

## Red Flags
[5-row table per spec §4.1]

## Steps

### STEP 1 — preflight
Read `_shared/roam/preflight.md`.

### STEP 2 — config gate (decomposed: 5 sub-steps)
Read `_shared/roam/config-gate/overview.md`. Then sequence through 5 sub-step
refs (backfill-env → detect-platform → enrich-env → confirm-env-model-mode →
persist-config). Each emits own marker.

### STEP 3 — discovery + briefs
Read `_shared/roam/discovery.md`. Use vg-load --index for surface enumeration.

### STEP 4 — spawn executors (3 branches)
Read `_shared/roam/spawn-executors.md`.

### STEP 5 — aggregate + analyze
Read `_shared/roam/aggregate-analyze.md`.

### STEP 6 — artifacts (spec.ts proposals)
Read `_shared/roam/artifacts.md`.

### STEP 7 — optional fix loop (existing subagent)
Read `_shared/roam/fix-loop.md`.

### STEP 8 — close
Read `_shared/roam/close.md`.
```

- [ ] **Step 2: Verify line count**

```bash
wc -l commands/vg/roam.md   # ≤ 600
```

- [ ] **Step 3: Commit**

```bash
git add commands/vg/roam.md
git commit -m "refactor(r3.5-roam): slim entry — 1103 → 500 lines

7 refs in _shared/roam/ + 1 nested config-gate/ dir (5 sub-steps).
0 new subagents (auto-fix loop subagent at step 7 preserved).
Mega-gate 0a_env_model_mode_gate (355 lines, 9 sub-prompts) decomposed
into 5 markers (audit FAIL #10 fix).
roam.native_tasklist_projected in must_emit_telemetry (PARTIAL audit
fix inherited via R1a). Phase F Task 30 absorbed for vg:roam (discovery
ref uses vg-load --index)."
```

---

## Phase D — Static tests

### Task 11: Static tests for slim size + structure + decomposition

**Files:**
- Create: `scripts/tests/test_roam_slim_size.py`
- Create: `scripts/tests/test_roam_references_exist.py`
- Create: `scripts/tests/test_roam_step_0a_decomposed.py`
- Create: `scripts/tests/test_roam_no_new_subagents.py`
- Create: `scripts/tests/test_roam_uses_vg_load.py`

- [ ] **Step 1: Write 5 tests**

```python
# test_roam_slim_size.py
def test_roam_md_under_600(): ...
def test_roam_md_uses_agent_not_task(): ...
def test_roam_md_lists_refs(): ...

# test_roam_references_exist.py — 7 refs + 6 config-gate sub-refs

# test_roam_step_0a_decomposed.py
def test_5_sub_step_markers_replace_single_0a():
    """The mega-gate decomposition must produce 5 markers (audit FAIL #10)."""
    text = Path("commands/vg/roam.md").read_text()
    expected = [
        "0a_backfill_env_pref",
        "0a_detect_platform_tools",
        "0a_enrich_env_options",
        "0a_confirm_env_model_mode",
        "0a_persist_config",
    ]
    for m in expected:
        assert m in text, f"Missing decomposed marker: {m}"
    # Original mega-gate marker should NOT appear
    assert "0a_env_model_mode_gate" not in text, (
        "Mega-gate marker still present — decomposition not complete"
    )

def test_each_sub_step_ref_under_150_lines():
    refs = [
        "commands/vg/_shared/roam/config-gate/backfill-env.md",
        "commands/vg/_shared/roam/config-gate/detect-platform.md",
        "commands/vg/_shared/roam/config-gate/enrich-env.md",
        "commands/vg/_shared/roam/config-gate/confirm-env-model-mode.md",
        "commands/vg/_shared/roam/config-gate/persist-config.md",
    ]
    for ref in refs:
        p = Path(ref)
        assert p.exists()
        assert len(p.read_text().splitlines()) <= 150

# test_roam_no_new_subagents.py
def test_no_new_roam_subagent_skill_files():
    """R3.5 is decomposition-only; no new agent SKILL.md should exist."""
    # Existing auto-fix subagent (if any) preserved at agents/<existing-name>/
    # Just ensure NO new vg-roam-* skill file
    new_subagent_paths = [
        "agents/vg-roam-config-gate",
        "agents/vg-roam-discovery",
        "agents/vg-roam-aggregator",
    ]
    for p in new_subagent_paths:
        assert not Path(p).exists(), f"Unexpected new subagent: {p}"

# test_roam_uses_vg_load.py
def test_discovery_ref_uses_vg_load():
    text = Path("commands/vg/_shared/roam/discovery.md").read_text()
    assert "vg-load" in text
    # Must NOT cat flat PLAN.md
    assert not re.search(r"cat\s+\S*PLAN\.md", text)
    assert not re.search(r"Read\s+\S*PLAN\.md", text)
```

- [ ] **Step 2: Run + commit**

```bash
pytest scripts/tests/test_roam_*.py -v
git add scripts/tests/test_roam_*.py
git commit -m "test(r3.5-roam): static tests — slim + refs + decomposition + no new subagents + vg-load"
```

---

### Task 12: Update emit-tasklist.py CHECKLIST_DEFS for vg:roam

**Files:**
- Modify: `scripts/emit-tasklist.py`

- [ ] **Step 1: Add 8 checklist groups**

```python
CHECKLIST_DEFS["vg:roam"] = [
    ("preflight",        ["0_parse_and_validate", "0aa_resume_check"]),
    ("config-gate",      ["0a_backfill_env_pref", "0a_detect_platform_tools", "0a_enrich_env_options", "0a_confirm_env_model_mode", "0a_persist_config"]),
    ("discovery",        ["1_discover_surfaces", "2_compose_briefs"]),
    ("execute",          ["3_spawn_executors", "4_aggregate_logs"]),
    ("analyze",          ["5_analyze_findings"]),
    ("artifacts",        ["6_emit_artifacts"]),
    ("fix-loop",         ["7_optional_fix_loop"]),
    ("close",            ["complete"]),
]
```

- [ ] **Step 2: Commit**

```bash
git add scripts/emit-tasklist.py
git commit -m "feat(r3.5-roam): emit-tasklist CHECKLIST_DEFS for vg:roam (8 groups)"
```

---

## Phase E — Sync + dogfood

### Task 13: Sync to PrintwayV3 + dogfood `/vg:roam <phase>`

- [ ] **Step 1: Update sync.sh + run pytest regression**

```bash
git add sync.sh
git commit -m "chore(r3.5-roam): sync.sh includes roam refs"

pytest scripts/tests/ -v 2>&1 | tee /tmp/r3.5-roam-pytest.log
```

- [ ] **Step 2: Sync + run dogfood**

```bash
cd /path/to/PrintwayV3
bash /Users/dzungnguyen/Vibe\ Code/Code/vgflow-bugfix/sync.sh
# Pick a phase where review + test BOTH PASSED
/vg:roam <phase>
```

- [ ] **Step 3: Verify 7 exit criteria (per spec §5.4)**

1. Tasklist visible immediately
2. **`roam.native_tasklist_projected` event ≥ 1** (PARTIAL → FULL fix)
3. **9 step markers touched** (8 hard + persist_config sub-step) — verifies decomposition (audit FAIL #10 fix)
4. SURFACES.md + RAW-LOG.jsonl + ROAM-BUGS.md + RUN-SUMMARY.json all written
5. roam.session.started + roam.session.completed + roam.analysis.completed events present
6. Lens dispatch happened (verify INSTRUCTION-{surface}-{lens}.md files generated)
7. Stop hook fires without exit 2

Critical: criterion 3 (decomposition must produce 5 sub-step markers, not 1 mega-gate marker).

- [ ] **Step 4: Verdict**

```bash
cat > docs/superpowers/specs/2026-05-03-vg-r3.5-roam-verdict.md <<EOF
# R3.5 Roam Pilot Verdict
**Date:** $(date -u +%Y-%m-%d)
**Phase:** ${PHASE}
[7 criteria PASS/FAIL]
## Critical: criterion 3 (5 sub-step markers)
[List of touched markers from events.db]
## Verdict: PASS | FAIL
## Phase F Task 30 update
After this verdict lands, update blueprint plan Phase F Task 30 to remove vg:roam from scope.
EOF

git add docs/superpowers/specs/2026-05-03-vg-r3.5-roam-verdict.md
git commit -m "docs(r3.5-roam): pilot dogfood verdict + 7 criteria evidence"
```

---

## Self-review notes

**Spec coverage check:**
- §1.5 goal "roam.md ≤500 lines" → Task 10 + Task 11
- §1.5 audit FAIL #10 (mega-gate decomposition) → Task 3 (5 sub-step refs) + Task 11 (assertion test)
- §1.5 audit PARTIAL #11 (tasklist projection) → fix inherited via R1a (Task 10 adds event to runtime_contract)
- §3 file layout (7 refs + 1 nested dir with 6 files) → Tasks 2-9
- §4.1 slim entry → Task 10
- §4.2 NO new subagents → Task 11 assertion (test_roam_no_new_subagents)
- §4.3 hooks SHARED with R1a → no new tasks
- §5.3 testing → Task 11
- §5.4 7 exit criteria → Task 13

**UX baseline coverage check:**
- Req 1 (per-task split): RAW-LOG.jsonl is per-tool subdir isolation (already split-aware); discovery uses vg-load --index (Task 4).
- Req 2 (spawn narration): Task 5 (spawn-executors), Task 8 (fix-loop) include narrate-spawn calls.
- Req 3 (compact hooks): inherits R1a stderr convention.

**Phase F Task 30 absorption check:**
- vg:roam portion covered by Task 4 (single L600 PLAN.md flat read replaced by vg-load --index).
- Task 11 test enforces no PLAN.md flat read in discovery.md.
- After R3.5 verdict (Task 13), update blueprint plan.

**Type/name consistency:**
- All step IDs match `commands/vg/roam.md` runtime_contract markers (verified Task 12).
- 5 sub-step markers replace single 0a_env_model_mode_gate (Task 3 + 11).
- NO new subagent names (existing auto-fix preserved).
- Helper names: shared with R1a.

**Placeholder scan:** none. Each Task has actual code/bash, file paths.

---

## Execution handoff

Plan saved to `docs/superpowers/plans/2026-05-03-vg-r3.5-roam-pilot.md`. Two execution options:

**1. Subagent-Driven (recommended)** — superpowers:subagent-driven-development.
**2. Inline Execution** — superpowers:executing-plans.

Pick 1 or 2.
