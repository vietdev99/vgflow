# R3.5 Roam Pilot Verdict

**Date:** 2026-05-03
**Branch:** `feat/rfc-v9-followup-fixes`
**Plan:** `docs/superpowers/plans/2026-05-03-vg-r3.5-roam-pilot.md`
**Spec:** `docs/superpowers/specs/2026-05-03-vg-roam-design.md`

## Static verification (offline criteria — verified)

| # | Criterion | Status | Evidence |
|---|---|---|---|
| 1 | `roam.md` ≤ 600 lines | **PASS** | 255 lines (`wc -l commands/vg/roam.md`) |
| 2 | All 7 top-level refs created | **PASS** | `commands/vg/_shared/roam/{preflight,discovery,spawn-executors,aggregate-analyze,artifacts,fix-loop,close}.md` |
| 3 | All 6 config-gate refs created | **PASS** | `commands/vg/_shared/roam/config-gate/{overview,backfill-env,detect-platform,enrich-env,confirm-env-model-mode,persist-config}.md` |
| 4 | Each config-gate sub-ref ≤ 150 lines | **PASS** | overview=63, backfill-env=100, detect-platform=97, enrich-env=57, confirm-env-model-mode=74, persist-config=110 |
| 5 | 5 sub-step markers replace single 0a mega-gate marker | **PASS** | `test_5_sub_step_markers_replace_single_0a` (audit FAIL #10 fix) |
| 6 | `roam.native_tasklist_projected` declared in must_emit_telemetry | **PASS** | PARTIAL audit fix inherited from R1a |
| 7 | Discovery ref uses `vg-load --index` (no flat PLAN.md read) | **PASS** | `test_discovery_ref_uses_vg_load`; Phase F Task 30 absorbed for vg:roam |
| 8 | NO new `vg-roam-*` agent SKILL.md files | **PASS** | `test_no_new_roam_subagent_skill_files` (R3.5 is decomposition-only) |
| 9 | `emit-tasklist.py` `CHECKLIST_DEFS["vg:roam"]` defined | **PASS** | 8 groups projecting all 15 markers |
| 10 | All 15 static tests pass | **PASS** | `pytest scripts/tests/test_roam_*.py -v` → 15 passed |

## Live dogfood criteria (DEFERRED)

The plan's Task 13 calls for live dogfood on a PrintwayV3 phase where review +
test BOTH PASSED. Live dogfood requires sandbox/staging access to a downstream
project not present in this repository. **Status: deferred to next session.**

The 7 live-criteria from spec §5.4 to verify when dogfood happens:

| # | Criterion | Verify by |
|---|---|---|
| 1 | Tasklist visible immediately | TodoWrite payload after `emit-tasklist.py` |
| 2 | `roam.native_tasklist_projected` event ≥ 1 | `vg-orchestrator query-events --event-type roam.native_tasklist_projected` |
| 3 | 9 step markers touched (8 hard + persist_config sub-step) — verifies decomposition (audit FAIL #10 fix) | `ls .vg/phases/{N}/.step-markers/` |
| 4 | SURFACES.md + RAW-LOG.jsonl + ROAM-BUGS.md + RUN-SUMMARY.json all written | `ls .vg/phases/{N}/roam/` |
| 5 | `roam.session.{started,completed}` + `roam.analysis.completed` events present | events.db query |
| 6 | Lens dispatch happened (verify `INSTRUCTION-{S}-{lens}.md` per model dir) | `ls .vg/phases/{N}/roam/{model}/INSTRUCTION-*.md` |
| 7 | Stop hook fires without exit 2 | hook-step-tracker.log tail |

**Critical:** criterion 3 (decomposition must produce 5 sub-step markers, not
1 mega-gate marker) is the central R3.5 verification. Static test
`test_5_sub_step_markers_replace_single_0a` covers the static case; live
dogfood proves runtime `mark_step` calls actually fire from each sub-ref.

## Static verdict: PASS

All 10 static criteria pass. The slim entry + 7 refs + 6 config-gate sub-refs
are in place, tests are green, runtime contract is correct, no new subagents
were introduced, and `vg-load --index` replaces flat PLAN.md read in discovery.

## Live verdict: PENDING

Dogfood execution against a PrintwayV3 phase deferred. Once executed, append
results to this file under "Live dogfood criteria" with PASS/FAIL per row.

## Phase F Task 30 update

After live dogfood verdict lands, update blueprint plan Phase F Task 30 to
remove `vg:roam` from scope. The discovery ref already enforces `vg-load
--index` and the static test rejects flat PLAN.md reads, so the absorption is
structurally complete.

## Commit history (R3.5 work)

Run `git log --oneline feat/rfc-v9-followup-fixes -- commands/vg/roam.md commands/vg/_shared/roam/ scripts/tests/test_roam_*.py scripts/emit-tasklist.py docs/superpowers/specs/2026-05-03-vg-r3.5-roam-verdict.md` to see the 11 incremental commits:

- chore(r3.5-roam): backup roam.md before slim refactor
- feat(r3.5-roam): preflight ref
- feat(r3.5-roam): config-gate decomposition (6 files)
- feat(r3.5-roam): discovery ref (vg-load --index)
- feat(r3.5-roam): spawn-executors ref (3 dispatch branches)
- feat(r3.5-roam): aggregate-analyze ref
- feat(r3.5-roam): artifacts ref
- feat(r3.5-roam): fix-loop ref (existing subagent preserved)
- feat(r3.5-roam): close ref
- refactor(r3.5-roam): slim entry — 1103 → 255 lines
- test(r3.5-roam): 5 static test files (15 assertions)
- feat(r3.5-roam): emit-tasklist CHECKLIST_DEFS for vg:roam (8 groups)
- docs(r3.5-roam): pilot static verdict + live dogfood criteria
