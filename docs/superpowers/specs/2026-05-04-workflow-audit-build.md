# Workflow audit: /vg:build

## Q1 Gap analysis
**Verdict: With fixes.** Runtime marker coverage is complete: `build.md` declares 20 `must_touch_markers` (`commands/vg/build.md:52`), and refs contain matching `step-active`/`mark-step` wrappers across preflight, context, blueprint validation, waves, post-exec, fix loop, pre-test, CrossAI, close (`commands/vg/_shared/build/preflight.md:32`, `commands/vg/_shared/build/context.md:33`, `commands/vg/_shared/build/validate-blueprint.md:29`, `commands/vg/_shared/build/waves-overview.md:86`, `commands/vg/_shared/build/post-execution-overview.md:75`, `commands/vg/_shared/build/in-scope-fix-loop.md:26`, `commands/vg/_shared/build/pre-test-gate.md:39`, `commands/vg/_shared/build/crossai-loop.md:114`, `commands/vg/_shared/build/close.md:155`). Strict XML audit is **no-go**: refs use markdown step bodies, not `<step name="...">`; existing tests still search XML for `8_execute_waves` (`scripts/tests/test_wave_verify_build_integration.py:23`).

## Q2 Loop/termination risk
**Verdict: With fixes.** Shell retry loops are bounded: wave debugger has `MAX_RETRIES=2` (`commands/vg/_shared/build/waves-overview.md:1042`), fix loop caps 3 attempts per warning (`commands/vg/_shared/build/in-scope-fix-loop.md:13`), and CrossAI caps a pass at 5 iterations (`commands/vg/_shared/build/crossai-loop.md:42`). Residual risk: CrossAI lets user continue with iterations 6-10 after exhaustion, with no documented global cap (`commands/vg/_shared/build/crossai-loop.md:165`).

## Q3 Hook token cost
**Verdict: Go.** Typical full run has 20 unique `step-active`, 20 unique `mark-step`, and 17 literal `emit-event` call sites in build refs; required start/tasklist/wave/completion events are declared in `build.md` (`commands/vg/build.md:98`) and emitted by run-start, tasklist, wave-start/wave-complete, pre-test, and close paths (`commands/vg/_shared/build/preflight.md:67`, `commands/vg/_shared/build/preflight.md:100`, `commands/vg/_shared/build/preflight.md:499`, `commands/vg/_shared/build/waves-overview.md:213`, `commands/vg/_shared/build/waves-overview.md:1194`, `commands/vg/_shared/build/pre-test-gate.md:217`, `commands/vg/_shared/build/close.md:190`). Bug D3 removed ~285-token taskboard re-render from step transitions (`scripts/vg-orchestrator/__main__.py:1131`, `scripts/vg-orchestrator/__main__.py:1492`). Residual hook noise: success path is near-zero stderr; blocks emit compact 3-line pointers (`scripts/hooks/vg-pre-tool-use-bash.sh:329`, `scripts/tests/test_spawn_guard_count_check.py:113`), user-prompt active-run reminder is ~300-700 bytes (`scripts/hooks/vg-user-prompt-submit.sh:38`), and SessionStart injects diagnostics only when open blocks exist (`scripts/hooks/vg-session-start.sh:132`).

## Q4 Role-standard compliance
**Verdict: With fixes.** Strong: capsule materialization before executor spawn (`commands/vg/_shared/build/waves-overview.md:392`), L1 pixel gate (`commands/vg/_shared/build/waves-overview.md:435`), L2 fingerprint fail-fast (`commands/vg/_shared/build/post-execution-overview.md:634`), post L3/L5/L6/truthcheck validation (`commands/vg/_shared/build/post-execution-overview.md:777`), per-wave reflection (`commands/vg/_shared/build/waves-overview.md:1265`), one commit per task (`commands/vg/_shared/build/waves-overview.md:686`), and final gates (`commands/vg/_shared/build/pre-test-gate.md:7`). Weak: post-executor single-spawn is prompt-enforced, not hook-enforced (`commands/vg/_shared/build/post-execution-overview.md:25`); scheduler serializes same-file conflicts, not explicit task dependency DAG (`commands/vg/_shared/build/waves-overview.md:273`); executor contract runs typecheck before commit but does not require red-test-first evidence (`commands/vg/_shared/build/waves-delegation.md:245`).

## Q5 Top 3 actionable gaps
1. **Critical** Post-executor spawn count is prompt-only; fix: add Stop/Agent guard requiring exactly one `vg-build-post-executor` before `9_post_execution` marker (`commands/vg/_shared/build/post-execution-overview.md:25`).
2. **Important** Dependency DAG is not enforced beyond same-file serialization; fix: parse task dependency edges from PLAN/capsules and block same-wave parallel spawn when an upstream task is unmet (`commands/vg/_shared/build/waves-overview.md:273`).
3. **Important** TDD is not first-class in executor contract; fix: add `test_cmd` + red/green evidence to task return schema and block commit without it (`commands/vg/_shared/build/waves-delegation.md:245`).

## Verdict
**Production-ready?** With fixes.
