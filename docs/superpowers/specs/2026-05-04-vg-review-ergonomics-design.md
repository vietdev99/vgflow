# `/vg:review` Ergonomics + Blueprint Wiring — Systematic Fix Design (7 bugs/gaps)

**Date:** 2026-05-04
**Source:** PV3 dogfood discovery (review run `4fe39741`, phase 4.1) + sếp Dũng's blueprint audit + Codex GPT-5.5 verification (89 findings, verdict ⛔ PAUSE → addressed in this revision)
**Status:** Revised v2 after Codex review — 5 design choices + RCRURDR correction + blueprint envelope gaps + FE contract gap. Awaiting re-approval.

---

## Goal

Fix 7 systemic UX + design failures spanning `/vg:review` and the blueprint→build envelope, all surfaced during PrintwayV3 (PV3) dogfood:

**Review-side (Tasks 33-36, "Track A"):**

1. **Bug A** — Review halts on detection without giving the user any choice; user must restart from scratch every iteration.
2. **Bug B** — Native tasklist contract is generated by the orchestrator but the AI agent never projects it via `TodoWrite`; user sees only a plain markdown table.
3. **Bug C** — Findings get ad-hoc IDs (e.g. AI invented "E-001" for a missing endpoint); user has no way to interpret prefixes. Codex round 1 caught 4 prefix collisions — see Bug C section for the corrected namespace.
4. **Bug D** — Review defers UI lens probes to `/vg:test` instead of running them; user reads "DEFERRED" rows instead of seeing actual UI bug findings. Task 26 (lens dispatch enforcement) shipped the infrastructure; Bug D wires it into review.md Phase 2.5.

**Build/blueprint-side (Tasks 37-39, "Track B"):**

5. **Bug E** — Blueprint produces UI-MAP / CRUD-SURFACES / LENS-WALK / RCRURD invariants but the build subagent envelope only carries some of them. Build FE/mutation tasks guess kit pattern + invariants instead of reading them.
6. **Bug F** — API-CONTRACTS.md is BE-only (4 blocks: auth, schemas, errors, sample). FE codegen has nothing to read for canonical URL, UI states, query param schema, cache invalidation, optimistic semantics, toast text. Result: "wrong URLs even though code đã gen rồi." Add BLOCK 5 = FE consumer contract.
7. **Bug G** — RCRURD invariant schema (Task 22) only handles ONE write+read cycle; the actual lens-form-lifecycle pattern is **R-C-R-U-R-D-R** (Read empty → Create → Read populated → Update → Read updated → Delete → Read after delete) = 7 ops, 4 reads. Extend schema with `lifecycle_phases[]` array.

Each bug is independent at the task level. Tracks A and B can land in parallel.

## Non-goals

- Refactor blocking-gate pattern outside `commands/vg/review.md` in this spec. Build / test / accept / scope use similar patterns and will inherit the wrapper, but their refactor is a separate follow-up plan once the wrapper proves itself in review.
- Migrate historical `REVIEW-FEEDBACK.md` files to the new finding-ID namespace. Forward-only — historical files stay as-is.
- Fix the underlying P3.5 carry-over bug in PV3 (missing `billing-summary` handler). That's the validation target for the new STEP 5.5 auto-fix-loop, not part of this design.
- **Generate typed FE API client (Path C-B / "Task 40").** Out of scope. Deferred to follow-up plan (`docs/superpowers/plans/<future>-typed-api-client.md`) after Tasks 37-39 telemetry shows whether contract-only fix (Bug F BLOCK 5) is sufficient. ESLint rule + FE migration are the costly pieces.
- **Wrapper rollout to build/test/accept/scope.** Pilot review only (Bug A). Generalization is a follow-up plan once `blocking-gate-prompt.sh` API stabilizes.

## Glossary (terms locked)

To avoid the 3-parallel-name confusion Codex finding #46 flagged:

- **RCRURDR** — canonical name for the 7-op lifecycle (Read empty → Create → Read populated → Update → Read updated → Delete → Read after delete). Used in spec, schema field `lifecycle: "rcrurdr"`, lens-form-lifecycle prompt, scanner-report-contract `goal_class`.
- **rcrurd** — the legacy single write+read invariant (Task 22 default). Schema field `lifecycle: "rcrurd"` (= just 1 phase: write + read + assert). Backward-compat default.
- **`crud-roundtrip`** — the transition kit name (existing, in `_shared/transition-kits/`). Treated as alias for RCRURDR. The kit's prompt instructs operators to capture all 7 lifecycle ops; the `goal_class: crud-roundtrip` in scanner-report-contract maps 1:1 to lifecycle: rcrurdr.
- **`lifecycle_phases`** — schema field name in `rcrurd_invariant.py` (extended in Task 39, NOT renamed). When `lifecycle: rcrurdr`, this list MUST contain exactly 7 phase entries.

## Bug A — Review hard-block without `AskUserQuestion`

### Current behavior

Review's preflight + Phase 2a.5 (API contract probe) + Phase 4 (drift / matrix / replay) detect issues, then:

```bash
echo "⛔ <gate> BLOCK — fix path: <hint>"
emit-event review.<X>_blocked
exit 1
```

Locations (grep `exit 1` in `commands/vg/review.md`): preflight_invariants_blocked, api_precheck_blocked, asserted_drift_blocked, matrix_staleness_blocked, replay_evidence_blocked, env_mode_gate, foundation_drift, plus 13+ more — ~20 paths total.

The user must read the fix hint, manually fix, then `/vg:review --retry-failed` from the start. PV3 events.db shows 5 review re-runs in one debug session before the user even understood E-001 was a missing endpoint.

### Design decision (Codex fix B2: 2-leg contract)

A bash script cannot invoke `AskUserQuestion` directly (that is a tool call from the AI controller, not a shell call). Wrapper is therefore split into **2 legs** the AI controller orchestrates:

**Leg 1 — `blocking_gate_prompt_emit`** (bash, called by the gate's calling site):
- Inputs: `<gate_id> <fix_hint_file> <severity> [evidence_path]`
- Output: structured JSON on stdout describing the 4 options (option keys + display text + subagent contract paths)
- Side effects: writes `.vg/runs/<run_id>/.gate-prompt-${gate_id}.json` with full prompt context for resume
- Exit code: 0 if prompt emitted successfully, 64 if input invalid (BSD sysexits convention reserves 64+ for wrapper internal errors)

**AI controller step** (between Leg 1 and Leg 2):
- Reads stdout JSON
- Invokes `AskUserQuestion` with the title + options
- Captures user's choice letter

**Leg 2 — `blocking_gate_prompt_resolve`** (bash, called after user answers):
- Inputs: `<gate_id> --user-choice=<a|s|r|x> [--override-reason=<text>]`
- Side effects: based on choice, dispatch:
  - `a` → spawn subagent (Agent tool call, instructed by AI controller per option JSON), wait for return, validate, re-emit (Leg 1) on UNRESOLVED for re-prompt
  - `s` → emit `override.used` + log debt + emit `review.gate_skipped_with_override`
  - `r` → emit `review.routed_to_amend` + write handoff message
  - `x` → emit `review.aborted_by_user` + clean exit
- Exit codes: 0 = fixed (gate now passes, calling site continues), 1 = skip-with-override (continue), 2 = route-to-amend (caller exits cleanly), 3 = abort (caller exits), 4 = re-prompt-needed (subagent UNRESOLVED, AI controller must re-call Leg 1 with appended repair packet)

### Option matrix

| Option | Effect |
|---|---|
| `[a] Auto-fix now` | AI controller spawns `general-purpose` subagent per the **NEW** `commands/vg/_shared/lib/review-fix-loop-delegation.md` contract (NOT the build-shaped Task 10 contract — review fixes against deployed app + artifacts, not source-file ownership window). Max 3 attempts. On PASS → re-run gate validator → if still fails, exit 4 (re-prompt with new repair packet). |
| `[s] Skip with override` | Emit `override.used --flag <skip-flag> --severity <wrapper-sev-mapped-to-debt-vocab>`, log to override-debt register, mark step done, continue. Requires `--override-reason=<text>` (≥50 chars). |
| `[r] Route to /vg:amend` | When the gate's root cause is a CONTEXT decision (e.g. drift register entry), exit review with handoff message: "this needs `/vg:amend <phase>` first". Telemetry: `review.routed_to_amend`. |
| `[x] Abort review` | Clean exit. Run-complete event emits with `outcome: aborted_by_user`. Stop hook accepts (no unhandled blocks because user explicitly chose abort). |

### Severity vocabulary mapping (Codex finding #76)

Wrapper input vocab is `error|warn|critical` (review.md gates). Override-debt vocab is `critical|high|medium`. Mapping:
- wrapper `critical` → debt `critical`
- wrapper `error` → debt `high`
- wrapper `warn` → debt `medium`

Locked in `commands/vg/_shared/lib/blocking-gate-prompt-contract.md`.

### `--non-interactive` mode (Codex finding #81)

When `$ARGUMENTS` contains `--non-interactive` (CI runs), Leg 1 short-circuits: skip AskUserQuestion entirely, behave as if user picked `[x] Abort review`. Emit `review.aborted_non_interactive_block` (warn-tier). Rationale: silent auto-fix in CI is dangerous; skip-with-override would silently accumulate debt. Loud abort is correct CI semantics.

### Pilot scope

Refactor `commands/vg/review.md` only — at every `*_blocked` event-emitting `exit 1` site (≈ 13 paths matching the frontmatter `must_emit_telemetry` warn-tier `review.<X>_blocked` declarations). Non-blocking `exit 1` paths (precondition guards, missing-file aborts) stay as-is. Build/test/accept generalization in follow-up plan.

### Selection rule for refactor sites (Codex finding #1)

A `exit 1` site is in scope IFF all three:
1. Immediately preceded by `emit-event "review.<X>_blocked"` call (or within 5 lines)
2. The `<X>` matches one of the 13 declared telemetry events: `api_precheck_blocked`, `preflight_invariants_blocked`, `matrix_staleness_blocked`, `matrix_evidence_link_blocked`, `evidence_provenance_blocked`, `asserted_drift_blocked`, `mutation_submit_blocked`, `rcrurd_preflight_blocked`, `rcrurd_depth_blocked`, `rcrurd_post_state_blocked`, `edge_case_variant_blocked`, `tasklist_projection_skipped` (Task 34's), `routed_to_amend` (Task 33's emit point — included for symmetry, not refactored)
3. Site has a user-facing fix-path message (`echo "⛔ ... fix:"` or similar)

Sites NOT in scope: missing-file guards, schema-load failures, internal assertion failures (these stay `exit 1` — they are bugs, not gate decisions).

### Subagent contract for option `[a]` (Codex fix B5)

NEW file: `commands/vg/_shared/lib/review-fix-loop-delegation.md`. Different from Task 10's build-fix-loop contract:

- Subagent runs against the **deployed app** when the gate category is API-related (`api_precheck`, `asserted_drift`, `replay_evidence`, `mutation_submit`). It can curl handlers, write FE/BE source code, restart API service.
- Subagent runs against **artifacts** when the gate is drift/matrix-related (`matrix_staleness`, `foundation_drift`, `rcrurd_post_state`) — edits CONTEXT.md, drift register, RUNTIME-MAP.json.
- Max 3 attempts. After attempt 3 → return `{"status": "UNRESOLVED", "iterations": 3, "repair_packet": {...}}`. Wrapper Leg 2 exits with code 4 (re-prompt).
- Phase-ownership allowlist: review subagent has WIDER scope than build subagent (deployed-app + artifacts) but still constrained to the current phase + its dependencies. Excludes other phases' artifacts.
- Forbidden: editing API-CONTRACTS.md → return UNRESOLVED with `blocked_by="contract_amendment_required"` → wrapper short-circuits to option `[r]` (auto-route to amend, no re-prompt).

## Bug B — Native tasklist not projected

### Current behavior

`commands/vg/review.md` writes `.vg/runs/<run_id>/tasklist-contract.json` (schema=`native-tasklist.v2`, populated checklists). The hook in `scripts/hooks/vg-pre-tool-use-bash.sh` ALREADY enforces evidence file existence before allowing any `step-active`. The hook's `Required fix` text already mentions `TodoWrite`. So the hook is not the bug — its diagnostic just isn't strong enough to interrupt AI's habit pattern.

PV3 events.db shows 15 historical `PreToolUse-tasklist` block.handled events. AI hits block, emits `vg.block.handled` (loop closes), then continues forward — but evidence file still missing, next `step-active` blocks again. AI ends up looping or abandoning step-active entirely.

### Design decision (Codex fix: hook diagnostic upgrade + slim-entry positioning + telemetry only)

The hook does NOT need new conditional logic — it already gates by evidence file. What's needed is:

1. **Slim-entry positioning** — review.md slim entry must reference `_shared/lib/tasklist-projection-instruction.md` BEFORE any `<step name=...>` block (currently the TASKLIST_POLICY section is at lines 268-327 but separated from the imperative). Test asserts byte position: instruction marker MUST appear before the first `<step ` tag (Codex finding #14 — first occurrence of "step-active" is too loose; tighten to `<step ` opening tag).
2. **Hook diagnostic upgrade** — replace existing `Required fix` text in `emit_block()` with a stronger, more imperative + more specific block: name the exact tool (`TodoWrite`), the exact subcommand (`vg-orchestrator tasklist-projected --adapter claude`), and the exact file path (the evidence file location). Test asserts the upgraded diagnostic contains the new exact phrase that did NOT appear in the old diagnostic — so red-then-green TDD works (Codex finding #11).
3. **New telemetry event** — `<command>.tasklist_projection_skipped` (warn-tier) emitted by hook on BLOCK. AI bypass attempts become observable in `/vg:gate-stats`.

### Path canonicalization (Codex finding #51)

Evidence file path = **`.vg/runs/<run_id>/.tasklist-projected.evidence.json`** (repo-relative, NOT `${PHASE_DIR}/.vg/runs/...`). The existing hook (`vg-pre-tool-use-bash.sh:23`) is repo-relative and that's correct. Spec was wrong; fixed throughout this revision.

### Multi-command effect

The hook fires for ALL commands (build/test/scope/accept use the same evidence-file check). When `command_from_run="vg:review"`, hook emits `review.tasklist_projection_skipped`. When `command_from_run="vg:build"`, hook emits `build.tasklist_projection_skipped`, etc. Single hook + per-command event = all VG commands gain projection telemetry simultaneously. (Codex finding #16: this is the right scope.)

### Test that AI bypass remains expensive (Codex finding #19)

Add test: simulate the bypass — AI emits `vg.block.handled` without creating evidence file. Re-call hook → must STILL block (because evidence file still missing). Asserts that emitting handled does NOT bypass the gate, only TodoWrite + orchestrator subcommand can.

## Bug C — Finding ID namespace (Codex fix B1: re-namespace to avoid collisions)

### Current behavior

When AI scanners emit a finding, they invent ad-hoc IDs: "E-001", "Issue #3", "Finding A". The user sees "E-001" in the verdict and doesn't know what category it is.

### Design decision (REVISED after Codex finding #20-24)

Original prefix set (`EP|D|R|G|F|S|T`) had **5 collisions** with existing IDs in the codebase:
- `F-001` already used in `scripts/replay-finding.py` for replay finding IDs
- `D-XX` already used as decision IDs (`P{phase}.D-XX`) in CONTEXT.md
- `G-XX` already used as goal IDs (`G-04`, `G-141`, `G-W10-05`) — and existing goals are 2-digit, the original `\d{3}` regex would have rejected them
- `R-XX` overlaps with `BR-` business rules
- `OD-XX` already used for override-debt entries

**Re-namespaced** to non-colliding prefixes:

| Prefix | Category | Source | Width |
|---|---|---|---|
| `EP-` | Endpoint | API contract probe (Phase 2a.5), call-graph (Task 3 L4a-i) | 3-digit |
| `DR-` | Drift | Asserted-rule drift, foundation drift, matrix staleness | 3-digit |
| `RV-` | Rule-violation | Bootstrap-rule violation, scope-matched rule (Task 11) | 3-digit |
| `GC-` | Goal-comparison | Goal-comparison miss (Phase 4) — distinct from goal authoring `G-XX` | 3-digit |
| `FN-` | Foundation | FOUNDATION-DRIFT entries — distinct from replay-finding `F-XXX` | 3-digit |
| `SC-` | Schema | Contract-shape mismatch (Task 4 L4a-ii) | 3-digit |
| `TM-` | Telemetry | Missing required event | 3-digit |

Validator regex: `^(EP|DR|RV|GC|FN|SC|TM)-\d{3}$` (all 2-letter prefixes, no 1-letter to keep visual distinction from existing `D-/G-/F-/R-` namespaces).

### Validator

`scripts/validators/verify-finding-id-namespace.py` — runs at end of Phase 4. Greps `REVIEW-FEEDBACK.md` for finding-section headers (regex `^### (EP|DR|RV|GC|FN|SC|TM)-\d{3}: ` per Codex finding #26 — JSONL would be cleaner but requires bigger schema migration; markdown-grep is the v1).

When AI emits a non-conforming ID (e.g. legacy `E-001`), validator emits `review.finding_id_invalid` (warn-tier) AND prints a fix-suggestion: "did you mean `EP-001`?" (mapping single-letter → 2-letter — Codex finding #28).

### Warn-tier rollout — observable success metric (Codex finding #27)

Promote validator from warn → BLOCK when:
- Zero `review.finding_id_invalid` events for 14 consecutive runs across ≥2 projects
- AND no manual override/skip used during that window

Tracked in `/vg:gate-stats finding-id-namespace --since 14d`. Promotion is operator-triggered (edit validator severity in code), not auto-time-based.

### Backward compat

Forward-only. Historical files stay as-is. Validator runs only on the current phase's REVIEW-FEEDBACK.md, not on archived ones.

## Bug D — Review defers UI lens probes to /vg:test (wire Task 26 into review.md Phase 2.5)

### Current behavior

PV3 review run output:

```
| Phase 2b-3 Goal sequence recording | ⏸ DEFERRED (multi-hour Playwright work) |
| Phase 2.5/2.7/2.9 Lens probes      | ⏸ DEFERRED                               |
| Phase 3 Fix loop                    | ⏸ DEFERRED (no errors actionable inline) |
```

UI bugs (form-lifecycle R8 update_did_not_apply, business-coherence drift, R9 ui_render_truth_mismatch) live in lens probes. Review hand-waves them to `/vg:test`, where the cycle restarts with no review record.

### Design decision (Codex fix B4: split into 36a + 36b)

Task 26 already shipped infrastructure (LENS-DISPATCH-PLAN.json schema, emit-dispatch-plan.py, verify-lens-runs-coverage.py, M1 tier dispatcher, M2 trace cross-check, lens-coverage-matrix.py) — but explicitly deferred review.md wiring per commit `ed4d148` reasoning "architectural — touches existing widely-used scripts".

Codex finding #66 quantified the deferred cost: 19 lens-prompt files × 6 frontmatter fields = **114 atomic edits**. That's its own task, separate from the wiring.

**Split into 2 tasks:**

- **Task 36a** — Lens prompt frontmatter migration. Add `recommended_worker_tier`, `worker_complexity_score`, `min_actions_floor`, `min_evidence_steps`, `required_probe_kinds`, `fallback_on_inconclusive` to all 19 `commands/vg/_shared/lens-prompts/lens-*.md` files. Field values per Task 26 spec table (haiku/sonnet/opus tiers per lens complexity). Pure additive, no logic change. Single commit.
- **Task 36b** — Wire Task 26 into review.md Phase 2.5. Modify `spawn_recursive_probe.py` (~200-400 LOC change per Codex finding #67) to: (a) call `emit-dispatch-plan.py` to write LENS-DISPATCH-PLAN.json, (b) iterate dispatches with `lens_tier_dispatcher.select_tier`, (c) write per-dispatch artifact at canonical `runs/<lens>/<goal>.json` path, (d) post-loop call `verify-lens-runs-coverage.py`. Coverage gate failure routes through Task 33's wrapper (4-option AskUserQuestion). LENS-COVERAGE-MATRIX.md rendered for user via Task 26's existing aggregator.

### Skip-mode interaction (Codex finding #32)

Existing `--probe-mode skip` writes `.recursive-probe-skipped.yaml`. Coverage gate guards: if `.recursive-probe-skipped.yaml` exists, gate skipped (legitimate user decision). This preserves existing escape hatch.

### BLOCK on MISSING vs wrapper-routing (Codex finding #31)

`verify-lens-runs-coverage.py` returns exit 1 when MISSING dispatches detected. Task 36b wires this into review.md as a Task 33 wrapper call site (gate_id = `lens_coverage_blocked`). User gets 4-option AskUserQuestion when MISSING:
- `[a] Auto-fix` → spawn subagent that runs the missing lens probes
- `[s] Skip with override` → emit override-debt, continue (lens findings missed)
- `[r] Route to /vg:amend` → not applicable (no decision change), wrapper short-circuits to `[s]`
- `[x] Abort` → standard

## Bug E — Build envelope incomplete (Codex finding #33: scope shrunk)

### Current state (Codex finding #33 corrected the conversation context)

`commands/vg/_shared/build/waves-delegation.md:30-31, 61-62` ALREADY pass `ui_map_path` + `view_components_path` in envelope and `@${PHASE_DIR}/UI-MAP.md (if exists)` in prompt template. So UI-MAP and VIEW-COMPONENTS are NOT the gap.

Actual missing artifacts (verified by grep):

1. **CRUD-SURFACES.md** — produced by blueprint, consumed only by `/vg:test`. Build mutation tasks don't see kit:crud-roundtrip pattern → handler shape drift.
2. **LENS-WALK seeds** — produced by blueprint Step `2b5e_a_lens_walk`, consumed only by `/vg:review`. Build can't pre-test against lens scenarios.
3. **RCRURD invariant per task** — produced by Task 22 schema (per-goal), but envelope has no field for per-task invariant slice. Build FE/mutation task subagent doesn't see invariants → forgets to insert `expectReadAfterWrite()` from Task 24 helper.

### Design decision (Codex finding #34-35: per-task slicing)

CRUD-SURFACES is per-phase but build envelope is per-task. Loading whole CRUD-SURFACES.md into every task's envelope inflates context budget. Solution: per-task slice via `vg-load`:
- `vg-load --phase N --artifact crud-surfaces --task NN` → returns only resources/operations the task touches
- `vg-load --phase N --artifact lens-walk --goal G-NN` → returns only lens variants for goals the task implements
- `vg-load --phase N --artifact rcrurd-invariant --task NN` → returns only invariants for goals the task touches

Orchestrator pre-resolves per-task slices BEFORE spawning the executor (Codex finding #35: subagent doesn't know goal→task mapping; orchestrator does). Slice paths added to envelope:

```json
"crud_surfaces_slice_path": ".vg/runs/.../task-NN.crud-surfaces.md",
"lens_walk_slice_path": ".vg/runs/.../task-NN.lens-walk.md",
"rcrurd_invariants_paths": [".vg/runs/.../task-NN.rcrurd.G-04.yaml", ".vg/runs/.../task-NN.rcrurd.G-12.yaml"]
```

### Backward compat (Codex finding #36)

For in-flight phases with stale capsules: `vg-build-task-executor` SKILL.md must tolerate missing fields with graceful degradation — log warning, continue with reduced context. Old phases keep working; new phases get richer envelope.

## Bug F — API-CONTRACTS.md is BE-only (FE Contract gap)

### Current state

`API-CONTRACTS.md` strictly 4 blocks per endpoint (`contracts-delegation.md` lines 116-185): auth+middleware, Zod schemas, error responses, test sample. All BE-flavored. FE codegen has nothing to read for canonical URL, UI states, query params, cache invalidation, optimistic semantics, toast text. Result: PV3 dogfood saw "wrong URLs even though code đã gen rồi" + missing UI loading/error states + scattered toast messages.

### Design decision (Path C-A: BLOCK 5 inline + 2-pass blueprint)

Add **BLOCK 5 = "FE consumer contract"** to API-CONTRACTS.md per endpoint. **2-pass blueprint** (Codex finding #61):
- **Pass 1** (existing `vg-blueprint-contracts` subagent) — generates BE 4 blocks unchanged. No FE context needed.
- **Pass 2** (NEW `vg-blueprint-fe-contracts` subagent) — runs AFTER `2b5e_a_lens_walk` step (so UI-MAP + VIEW-COMPONENTS exist). Reads BE 4 blocks + UI-MAP + VIEW-COMPONENTS. Emits BLOCK 5 per endpoint. Orchestrator merges into API-CONTRACTS.md (or per-endpoint `API-CONTRACTS/<slug>.md` files).

### BLOCK 5 fields (Codex finding #39 — expanded with 8 additional fields)

```typescript
// === BLOCK 5: FE consumer contract ===
export const PostSitesFEContract = {
  // Codex finding #39: must include enough fields to prevent FE drift
  url: '/api/sites',                                    // canonical, FE typed client imports verbatim (no paraphrase)
  consumers: ['SiteListPage', 'CreateSiteModal'],       // component names (best-effort, may be regenerated post-blueprint scan)
  ui_states: {                                          // FE rendering contract
    loading: 'spinner-with-skeleton',
    error: 'inline-banner-with-retry',
    empty: 'cta-create-first-site',
    success: 'toast-success-then-redirect',
  },
  query_param_schema: { 'page-size': 'number 1..100', q: 'string optional' },
  invalidates: ['GetSites', 'GetSiteCount'],            // mutation cache invalidation map
  optimistic: false,                                    // FE update strategy
  toast_text: { success: 'Site created', error_403: 'Need publisher role' },
  navigation_post_action: 'navigate:/sites/{id}',       // Codex finding #41: must be consistent with BE Location header
  // Codex finding #39 additions:
  auth_role_visibility: ['publisher'],                  // FE conditional render guard
  error_to_action_map: { 401: 'navigate:/login', 429: 'show-retry-after', 403: 'modal:contact-admin' },
  pagination_contract: { type: 'cursor', cursor_field: 'next_cursor' } as const | null,
  debounce_ms: null,                                    // for search/filter only
  prefetch_triggers: [] as string[],                    // ['hover:link:/sites/{id}', 'mount:parent:Dashboard']
  websocket_correlate: null,                            // WS event topic that invalidates this query, e.g. 'sites.created'
  request_id_propagation: false,                        // FE must propagate response.request_id to follow-ups
  form_submission_idempotency_key: 'header:Idempotency-Key',  // null for non-mutation endpoints
} as const;
```

Total **16 fields**. Validator enforces per-method matrix (e.g. `pagination_contract` required only for `GET` list endpoints, `form_submission_idempotency_key` required only for `POST/PUT/PATCH` mutation endpoints).

### Inline vs separate file decision (Codex finding #40)

Inline (BLOCK 5 in same file) chosen IFF a strong validator can be authored in Task 38 that fails on missing BLOCK 5. Validator is part of Task 38 scope. If validator implementation slips, fall back to separate `${PHASE_DIR}/FE-CONTRACTS/<slug>.md` files (loud-fail on absence is more robust than silent BLOCK 5 absence).

### `consumers` field churn (Codex finding #64)

List patterns instead of literal component names where possible:
```typescript
consumers: ['apps/web/src/sites/**/*.tsx'],   // glob pattern preferred
// or
consumers_auto_scan: true,                    // post-blueprint scan populates this
```

### Retroactivity (Codex finding #42)

PV3 phase 4.1 has API-CONTRACTS.md without BLOCK 5. Two options:
1. Backfill via one-pass blueprint re-run (`/vg:blueprint 4.1 --only=fe-contracts`).
2. Effective-for-new-phases-only.

**Spec choice: option 1**. After Task 38 lands, sếp can run `/vg:blueprint <phase> --only=fe-contracts` to backfill BLOCK 5 for any in-flight phase. Validator runs against BLOCK 5 presence, not against the existence of phase predating Task 38.

## Bug G — RCRURDR full lifecycle (Codex fix: extend in place, not rename)

### Current state

`scripts/lib/rcrurd_invariant.py` schema:
- `RCRURDInvariant` dataclass holds 1 write + 1 read + assertions + preconditions + side_effects + ui_assert
- Single `extract_from_test_goal_md` regex matches one ```yaml-rcrurd``` fence

This handles 1-cycle invariants (PATCH → GET) but not the full lens-form-lifecycle pattern: **R-C-R-U-R-D-R** (Read empty → Create → Read populated → Update → Read updated → Delete → Read after delete) = 7 ops, 4 reads.

### Design decision (Codex finding #55-58: extend in place)

**Do NOT rename `rcrurd_invariant.py` → `rcrurdr_invariant.py`.** That breaks Task 23/24/25 callsites. Extend in place:

Schema gains a `lifecycle` discriminator + `lifecycle_phases[]` array:

```yaml
goal_type: mutation
lifecycle: rcrurdr  # NEW: "rcrurd" (default, single-cycle, backward-compat) | "rcrurdr" | future "rcurdr" / etc.

# Backward-compat: when lifecycle == "rcrurd" (default), the existing fields apply:
read_after_write_invariant:    # legacy, optional when lifecycle != "rcrurd"
  write: ...
  read: ...
  assert: [...]

# NEW: when lifecycle == "rcrurdr", lifecycle_phases is required + must contain 7 entries
lifecycle_phases:
  - phase: read_empty
    read: { method: GET, endpoint: /api/users, cache_policy: no_store, settle: {mode: immediate} }
    assert: [{ path: $.users, op: equals, value_from: literal:[] }]
  - phase: create
    write: { method: POST, endpoint: /api/users }
    read:  { method: GET, endpoint: /api/users, cache_policy: no_store, settle: {mode: immediate} }
    assert: [{ path: $.users[0].id, op: matches, value_from: literal:^[a-f0-9-]{36}$ }]
  - phase: read_populated
    read: { method: GET, endpoint: /api/users/{created_id}, cache_policy: no_store, settle: {mode: immediate} }
    assert: [{ path: $.id, op: equals, value_from: action.created_id }]
  - phase: update
    write: { method: PATCH, endpoint: /api/users/{created_id}, cache_policy: no_store }
    read:  { method: GET, endpoint: /api/users/{created_id}, cache_policy: no_store, settle: {mode: immediate} }
    assert: [{ path: $.email, op: equals, value_from: action.new_email }]
  - phase: read_updated
    read: { method: GET, endpoint: /api/users/{created_id}, cache_policy: no_store, settle: {mode: immediate} }
    assert: [{ path: $.email, op: equals, value_from: action.new_email }]
  - phase: delete
    write: { method: DELETE, endpoint: /api/users/{created_id} }
    read: { method: GET, endpoint: /api/users/{created_id}, cache_policy: no_store, settle: {mode: immediate} }
    assert: [{ path: $.error.code, op: equals, value_from: literal:NOT_FOUND }]
  - phase: read_after_delete
    read: { method: GET, endpoint: /api/users, cache_policy: no_store, settle: {mode: immediate} }
    assert: [{ path: $.users[?(@.id==created_id)], op: equals, value_from: literal:null }]
```

### Goal-type → required-phases (Codex finding #57)

```yaml
# Sub-lifecycle goals only need a subset:
goal_type: create_only      # phases: [read_empty, create, read_populated]
goal_type: update_only      # phases: [read_populated, update, read_updated]
goal_type: delete_only      # phases: [read_populated, delete, read_after_delete]
goal_type: crud_full        # = rcrurdr (all 7)
goal_type: mutation         # legacy single read_after_write_invariant (lifecycle: "rcrurd")
```

Schema validator enforces the required-subset per goal_type. Single-cycle `mutation` goals stay backward-compatible.

### `ui_assert` per-phase (Codex finding #47)

Existing `ui_assert` block keys to a single read response. RCRURDR has 4 reads; ui_assert must specify which read it applies to:

```yaml
ui_assert:
  apply_to_phase: read_populated   # NEW: which lifecycle phase this assertion attaches to
  ops: [...]
```

When `lifecycle: rcrurd` (legacy), `apply_to_phase` defaults to the single read.

### Migration for Tasks 23-25 (Codex finding #58)

- **Task 23 (review runtime gate)**: when `lifecycle == "rcrurdr"`, iterate over `lifecycle_phases`, run write+read+assert per phase. Preserves legacy single-cycle path.
- **Task 24 (codegen helper)**: emit `expectLifecycleRoundtrip(lifecycle_phases, page, request)` when `lifecycle: rcrurdr`; legacy `expectReadAfterWrite(...)` when `lifecycle: rcrurd`.
- **Task 25 (R9 ui_assert)**: ui_assert ops keyed by `apply_to_phase`. Per-phase DOM assertions.

All 3 callsites updated in **Task 39**, atomic.

### Glossary alignment (Codex finding #46)

`crud-roundtrip` (transition kit, exists) ↔ `lifecycle: rcrurdr` ↔ `goal_class: crud-roundtrip` (scanner-report-contract). All three terms refer to the same 7-op pattern. Spec Glossary section locks this.

---

## Architecture summary (revised)

```
review.md slim entry (Tracks A — Tasks 33-36)
  ├── run-start
  ├── [Task 34] read tasklist-contract.json + TodoWrite + tasklist-projected
  │      ↑ enforced by existing PreToolUse-bash hook (diagnostic upgraded, telemetry added)
  ├── step-active 0a / 0b / 0c (preflight)
  │      └── on BLOCK detected → [Task 33] blocking-gate-prompt-emit (bash)
  │            └── AI controller calls AskUserQuestion (4 options)
  │                 └── [Task 33] blocking-gate-prompt-resolve --user-choice=<a|s|r|x>
  │                       ├── [a] auto-fix subagent (review-fix-loop-delegation, max 3, deployed-app + artifacts scope)
  │                       │      └── UNRESOLVED → exit 4 → AI re-calls Leg 1 with repair packet
  │                       ├── [s] skip-with-override (vocab-mapped to override-debt schema)
  │                       ├── [r] route to /vg:amend → exit clean
  │                       └── [x] abort → run-complete with aborted_by_user
  ├── ... other gates same pattern (13 total per selection rule above) ...
  ├── [Task 36b] Phase 2.5 lens dispatch enforcement
  │      ├── emit-dispatch-plan.py → LENS-DISPATCH-PLAN.json
  │      ├── spawn_recursive_probe.py iterates dispatches w/ tier dispatcher
  │      ├── verify-lens-runs-coverage.py → BLOCK on MISSING → routes through Task 33 wrapper
  │      └── lens-coverage-matrix.py renders LENS-COVERAGE-MATRIX.md
  ├── Phase 4 goal_comparison
  │      └── [Task 35] verify-finding-id-namespace.py validates EP|DR|RV|GC|FN|SC|TM-NNN prefixes
  └── close

blueprint.md slim entry (Track B — Tasks 37-39)
  ├── ... existing steps unchanged ...
  ├── 2a_plan, 2b_contracts (Pass 1 — BE 4 blocks)
  ├── 2b5e_edge_cases, 2b5e_a_lens_walk (existing)
  ├── [NEW Task 38] 2b6_fe_contracts (Pass 2 — BLOCK 5)
  │      └── vg-blueprint-fe-contracts subagent reads UI-MAP + VIEW-COMPONENTS + BE 4 blocks → emits BLOCK 5
  ├── 2b7_rcrurdr_invariants (Task 22 schema, extended by Task 39)
  └── close

build.md task envelope (Track B — Task 37)
  ├── Existing fields: plan_task_path, contract_slice_paths, ui_map_path, view_components_path, edge_cases_for_goals
  ├── [Task 37] crud_surfaces_slice_path (per-task slice via vg-load)
  ├── [Task 37] lens_walk_slice_path (per-task slice)
  └── [Task 37] rcrurd_invariants_paths (per-task list of per-goal invariants)
```

## Component boundaries

- `commands/vg/_shared/lib/blocking-gate-prompt-contract.md` (NEW) — 2-leg API spec for `blocking_gate_prompt_emit` + `blocking_gate_prompt_resolve`. Documents exit codes 0/1/2/3/4, severity vocab mapping, --non-interactive behavior.
- `scripts/lib/blocking-gate-prompt.sh` (NEW) — 2 bash functions implementing the contract.
- `commands/vg/_shared/lib/review-fix-loop-delegation.md` (NEW) — review-shaped subagent contract (deployed-app + artifacts scope), distinct from Task 10's build-shaped contract.
- `commands/vg/_shared/lib/tasklist-projection-instruction.md` (NEW) — canonical embed block for slim entries (Task 34).
- `scripts/validators/verify-finding-id-namespace.py` (NEW) — Python validator for EP|DR|RV|GC|FN|SC|TM-NNN regex.
- `scripts/lib/scanner_report_contract.py` (NEW — does NOT exist; spec was wrong) — Python parser/validator complementing the existing `commands/vg/_shared/scanner-report-contract.md`.
- `scripts/hooks/vg-pre-tool-use-bash.sh` — modify existing (no new hook): upgrade `Required fix` block, add `<command>.tasklist_projection_skipped` event emission. Path semantics: repo-relative `.vg/runs/<run_id>/...` (Codex finding #51).
- `scripts/spawn_recursive_probe.py` — modify existing (Task 36b): add Task 26 dispatch chain (~200-400 LOC).
- `commands/vg/_shared/lens-prompts/lens-*.md` — frontmatter additions (Task 36a, 19 files × 6 fields).
- `agents/vg-blueprint-fe-contracts/` (NEW — Task 38) — Pass 2 subagent + delegation contract.
- `scripts/lib/rcrurd_invariant.py` — extend in place with `lifecycle_phases[]` array (Task 39).

## Telemetry events declared (Codex finding #53)

All new events MUST be added to `must_emit_telemetry` blocks in their respective slim entries (otherwise Stop hook silent-skip):

In `commands/vg/review.md`:
- `<command>.tasklist_projection_skipped` (warn, Task 34)
- `review.gate_skipped_with_override` (warn, Task 33)
- `review.gate_autofix_attempted` (warn, Task 33 — Codex finding #8)
- `review.routed_to_amend` (warn, Task 33)
- `review.aborted_by_user` (warn, Task 33)
- `review.aborted_non_interactive_block` (warn, Task 33)
- `review.finding_id_invalid` (warn, Task 35)
- `review.lens_dispatch_emitted` (info, Task 36b)
- `review.lens_coverage_blocked` (warn, Task 36b)

In `commands/vg/blueprint.md`:
- `blueprint.fe_contracts_pass_completed` (info, Task 38)
- `blueprint.rcrurdr_invariant_emitted` (info, Task 39)

In `commands/vg/build.md`:
- `build.envelope_slice_resolved` (info, Task 37)

## Sequencing & dependency graph (Codex finding #68-71)

```
Track A (review pilot, sequential):
  34 (tasklist) → 33 (wrapper) → 35 (namespace) → 36a (lens frontmatter) → 36b (lens wiring)
  ^                                                                            ^
  |                                                                            depends on 33 (uses wrapper for BLOCK)
  +-- pure additive (hook diagnostic + slim entry positioning + telemetry); independent

Track B (blueprint/build envelope, partly parallel):
  39 (RCRURDR schema)          ──┐
  38a (BLOCK 5 schema)         ──┤
                                  ├──> 37 (envelope completeness — depends on 38a + 39 schemas)
                                  └──> 38b (BLOCK 5 validator + Pass 2 subagent — depends on 38a)

Cross-track:
  36b (Track A) needs Task 33's wrapper (Track A predecessor) — already covered by Track A's order.
  37 (Track B) is independent of Track A.
```

Suggested execution: Tracks A and B run in parallel by 2 sessions. Within each track, the order above. **No cross-track blockers** for now.

Plan format: `superpowers:writing-plans` per `docs/superpowers/plans/2026-05-04-vg-review-ergonomics.md` — same flow as Tasks 1-32. **8 tasks total** (33, 34, 35, 36a, 36b, 37, 38a/38b combined, 39).

## Error handling

- Wrapper subagent UNRESOLVED: exit 4 → AI controller re-calls Leg 1 with repair packet appended to fix-hint. User picks again. No infinite loop because each `[a]` is a fresh subagent invocation; sếp can pick `[s]` or `[x]` at any iteration.
- TodoWrite hook bypass attempt: hook BLOCKs with upgraded diagnostic. AI MUST call TodoWrite + tasklist-projected to clear (emitting `vg.block.handled` does not). After 3+ block-fired-without-resolution events same gate same run → block-correlator (Task 32) flags as RECURRING for manual intervention.
- Finding-ID validator: warn-tier means non-conforming → emit event, don't fail review.
- Build envelope graceful degradation: stale capsules from in-flight phases get warning log, executor continues with reduced context.
- BLOCK 5 missing on a new endpoint: validator (Task 38b) fails blueprint with fix-path "run `/vg:blueprint <phase> --only=fe-contracts` to regenerate Pass 2".
- RCRURDR phase mismatch (e.g. lifecycle: rcrurdr but lifecycle_phases has 5 entries): schema validator fails parse, emits clear error.

## Testing plan

Each task gets a self-contained test under `tests/`:

- `tests/test_blocking_gate_prompt.py` — Leg 1 + Leg 2 contract test, severity vocab mapping, --non-interactive auto-abort, exit code 4 re-prompt path.
- `tests/test_review_tasklist_projection.py` — bypass-test (handled without TodoWrite still blocks), upgraded diagnostic asserts NEW string, telemetry emitted, slim entry positioning byte test.
- `tests/test_finding_id_namespace.py` — non-conforming → warn telemetry + fix-suggestion mapping (E-001 → EP-001), conforming pass.
- `tests/test_lens_prompt_frontmatter.py` (Task 36a) — all 19 lens-*.md files contain the 6 required fields.
- `tests/test_review_lens_dispatch_wiring.py` (Task 36b) — synthetic phase, run review Phase 2.5, assert LENS-DISPATCH-PLAN.json emitted, dispatches iterated, coverage gate fires on missing.
- `tests/test_build_envelope_slices.py` (Task 37) — orchestrator pre-resolves per-task slices for crud-surfaces + lens-walk + rcrurd-invariants.
- `tests/test_blueprint_fe_contracts_pass.py` (Task 38a/38b) — synthetic blueprint, Pass 2 subagent reads UI-MAP, emits BLOCK 5 with all 16 fields per method matrix; validator catches missing fields.
- `tests/test_rcrurdr_lifecycle.py` (Task 39) — 7-phase RCRURDR doc parses + validates; goal_type → required_phases enforcement; ui_assert apply_to_phase keying; legacy single-cycle invariants still parse.

Plus integration test `tests/test_review_p4_dogfood_replay.py`: synthetic phase with 1 missing endpoint, run review, assert wrapper triggers + 4 options present + AskUserQuestion JSON shape correct.

## Task breakdown (revised)

| Task | Surface | Track | Files |
|---|---|---|---|
| **Task 34** | Bug B — slim entry repositioning + hook diagnostic upgrade + telemetry | A | `_shared/lib/tasklist-projection-instruction.md`, `vg-pre-tool-use-bash.sh`, `review.md`, `tests/test_review_tasklist_projection.py` |
| **Task 33** | Bug A — 2-leg blocking gate wrapper + refactor 13 review *_blocked sites | A | `_shared/lib/blocking-gate-prompt-contract.md`, `_shared/lib/review-fix-loop-delegation.md`, `scripts/lib/blocking-gate-prompt.sh`, `review.md`, `tests/test_blocking_gate_prompt.py` |
| **Task 35** | Bug C — finding ID namespace EP/DR/RV/GC/FN/SC/TM | A | `scripts/lib/scanner_report_contract.py`, `scripts/validators/verify-finding-id-namespace.py`, `_shared/scanner-report-contract.md`, `tests/test_finding_id_namespace.py` |
| **Task 36a** | Bug D — lens prompt frontmatter migration (19 files × 6 fields) | A | `_shared/lens-prompts/lens-*.md`, `tests/test_lens_prompt_frontmatter.py` |
| **Task 36b** | Bug D — wire Task 26 lens dispatch into review.md Phase 2.5 | A | `scripts/spawn_recursive_probe.py`, `review.md`, `tests/test_review_lens_dispatch_wiring.py` |
| **Task 37** | Bug E — build envelope completeness (per-task slices) | B | `_shared/build/waves-delegation.md`, `scripts/vg-load.sh`, `tests/test_build_envelope_slices.py` |
| **Task 38** | Bug F — BLOCK 5 schema + Pass 2 vg-blueprint-fe-contracts subagent + validator | B | `agents/vg-blueprint-fe-contracts/`, `_shared/blueprint/contracts-overview.md`, `scripts/validators/verify-fe-contract-block5.py`, `tests/test_blueprint_fe_contracts_pass.py` |
| **Task 39** | Bug G — extend rcrurd_invariant.py with `lifecycle_phases[]` + Tasks 23/24/25 callsite updates | B | `scripts/lib/rcrurd_invariant.py`, `scripts/validators/verify-rcrurd-runtime.py`, `scripts/codegen-helpers/expectReadAfterWrite.ts`, `tests/test_rcrurdr_lifecycle.py` |

8 tasks total. Tasks 33-36 (Track A) sequential within track. Tasks 37-39 (Track B) — Task 39 + Task 38a (schema) can run parallel; Task 37 + Task 38b (validator) depend on the schemas.

## Open follow-ups (not in this spec)

- **Task 40 / Path C-B** (deferred to future plan): generate typed FE API client from API-CONTRACTS BLOCK 2 + 5. Lint rule blocks hardcoded URLs in axios/fetch. Migration guide for existing FE code. Out of scope here; future plan based on Tasks 37-39 telemetry.
- **Wrapper rollout to build/test/accept/scope**: pilot review only. Generalization is its own plan.
- **Finding-ID promotion to BLOCK**: warn → BLOCK after observable success metric (zero `review.finding_id_invalid` for 14 consecutive runs, ≥2 projects).
- **Task 34's PV3 validation**: re-run `/vg:build 4.1 --only=post-execution` after Tasks 33-39 land. L4a-i FE→BE call graph (Task 3) should catch the same `billing-summary` missing endpoint as IN_SCOPE evidence, fed into STEP 5.5 auto-fix-loop. Confirms full chain end-to-end.
- **`/vg:gate-stats` schema update**: 12 new event types declared. Verify gate-stats CLI parses dot-and-underscore mixed names.
- **CLAUDE.md project doc**: describe the new `/vg:review` BLOCK behavior (4-option AskUserQuestion).
