**Project Positioning:** L3 Team default.

### Finding 1: Review Artifacts Not Gated Before Test-Spec
**Q**: 1, 7  
**File:line**: `commands/vg/test-spec.md:153-168`, `scripts/generate-deep-test-specs.py:407-424`  
**Purpose**: test-spec claims post-review consumption of `RUNTIME-MAP`.  
**Actual**: gate checks only build evidence + `TEST-GOALS`; generator builds from phase docs + static scan.  
**Gap**: `RUNTIME-MAP.json`, `GOAL-COVERAGE-MATRIX.md`, `.scan/scan-*.json`, `MATRIX-INTENT.json` can be absent or ignored.  
**Severity**: critical  
**Fix**: make review artifacts required inputs before generation; fail if missing/stale.

### Finding 2: `GOAL-COVERAGE-MATRIX.json` Contract Drift
**Q**: 1, 6, 7  
**File:line**: `commands/vg/_shared/review/close.md:79-91`, `commands/vg/test-spec.md:436-460`, `scripts/generate-matrix-intent.py:39-42`  
**Purpose**: codegen smart-routing uses matrix JSON.  
**Actual**: review close requires only `GOAL-COVERAGE-MATRIX.md`; test-spec passes `.json`; matrix-intent generator also requires `.json`.  
**Gap**: JSON matrix may never exist; downstream silently falls back or fails.  
**Severity**: critical  
**Fix**: make review write canonical `GOAL-COVERAGE-MATRIX.json`, derive md from it, and consume only JSON downstream.

### Finding 3: Edge Cases Not Generated By Deterministic Test-Spec
**Q**: 2, 4, 5  
**File:line**: `scripts/generate-lifecycle-specs.py:733-749`, `scripts/generate-deep-test-specs.py:421-433`  
**Purpose**: generate deep specs.  
**Actual**: emitted lifecycle source artifacts are `TEST-GOALS`, API mentions, phase context; no edge-case expansion.  
**Gap**: boundary values, empty strings, unicode, special chars, large payloads are absent unless already in upstream artifacts.  
**Severity**: critical  
**Fix**: add first-class `edge_cases[]` per goal in `LIFECYCLE-SPECS.json` and `CODEGEN-MANIFEST.json`.

### Finding 4: Negative Paths Are Prompt-Only
**Q**: 3, 4, 5  
**File:line**: `scripts/generate-lifecycle-specs.py:615-620`, `commands/vg/_shared/test/codegen/delegation.md:231-235`  
**Purpose**: include failure assertions.  
**Actual**: lifecycle evidence says “expected 4xx” inside happy lifecycle stages; codegen says never invent assertions beyond `TEST-GOALS`.  
**Gap**: no required `401/403/422`, validation failure, auth failure, timeout, network-error tests.  
**Severity**: critical  
**Fix**: add `negative_specs[]` contract with status, payload, actor, expected error shape, and mandatory manifest entries.

### Finding 5: Failure-Mode Specs Live In Optional Deep-Probe
**Q**: 2, 3, 4, 5  
**File:line**: `commands/vg/_shared/test/codegen/deep-probe.md:22-28`, `commands/vg/_shared/test/codegen/deep-probe.md:110-129`  
**Purpose**: generate edge/failure variants.  
**Actual**: deep-probe can generate 3 variants, but it is config-gated and skips when disabled or no READY goals.  
**Gap**: race, concurrent write, malformed payload, rate-limit, auth-negative are optional, not required in manifest.  
**Severity**: high  
**Fix**: move critical negative/failure variants into mandatory test-spec generation; leave deep-probe as extra.

### Finding 6: Edge-Case Gate Is Dead Unless Env Vars Exist
**Q**: 2, 5  
**File:line**: `commands/vg/_shared/test/codegen/delegation.md:386-420`  
**Purpose**: verify each `EDGE-CASES` variant appears in generated specs.  
**Actual**: gate depends on `EDGE_CASES_AVAILABLE`, `GOALS_LIST`, `ALLOW_SKIP`; search shows no orchestrator setup outside prompt doc.  
**Gap**: edge coverage gate can be skipped by unset env.  
**Severity**: critical  
**Fix**: compute these vars in orchestrator, always run validator when `EDGE-CASES/G-*.md` exists.

### Finding 7: `EDGE-CASES` Comes From Blueprint, Not Test-Spec
**Q**: 2, 5  
**File:line**: `commands/vg/_shared/blueprint/edge-cases.md:1-17`, `scripts/edge-cases-to-spec.py:2-11`  
**Purpose**: convert edge contract to tests.  
**Actual**: blueprint owns edge-case generation; test-spec only may consume existing variants.  
**Gap**: if blueprint skipped or legacy phase lacks `EDGE-CASES`, test-spec falls back to single-path spec.  
**Severity**: high  
**Fix**: test-spec must generate missing edge-case contract or block unless explicit override debt exists.

### Finding 8: Status Model Mismatch Drops READY_STRUCTURAL / READY_BEHAVIORAL
**Q**: 6  
**File:line**: `commands/vg/_shared/review/matrix-intent.md:3-12`, `commands/vg/_shared/test/codegen/delegation.md:195-202`  
**Purpose**: route review verdicts into codegen.  
**Actual**: review defines `READY_STRUCTURAL` / `READY_BEHAVIORAL`; codegen branch table only handles bare `READY`.  
**Gap**: structural/behavioral READY goals may not generate full specs.  
**Severity**: critical  
**Fix**: normalize statuses before codegen and require explicit disposition for every status.

### Finding 9: NOT_SCANNED Is Blocked Only In Test, Not Test-Spec
**Q**: 6  
**File:line**: `commands/vg/_shared/test/preflight.md:194-230`, `commands/vg/_shared/test/codegen/delegation.md:195-202`  
**Purpose**: prevent test from covering unresolved review gaps.  
**Actual**: `/vg:test` blocks `NOT_SCANNED`; test-spec codegen status table omits it.  
**Gap**: test-spec can generate artifacts without resolving `NOT_SCANNED`, then test blocks later.  
**Severity**: high  
**Fix**: test-spec must fail fast on `NOT_SCANNED` and route back to review.

### Finding 10: Stage Coverage Validator Reads Wrong Field
**Q**: 5, 8  
**File:line**: `scripts/generate-lifecycle-specs.py:693-696`, `scripts/validators/verify-spec-stage-coverage.py:115-135`  
**Purpose**: block shallow generated specs.  
**Actual**: lifecycle emits `steps`; validator reads `stages`. Required stage list becomes empty.  
**Gap**: shallow happy-path specs can pass Batch 23.  
**Severity**: critical  
**Fix**: validator must read `steps[].stage`; add regression test with missing `delete/read_after_delete`.

### Finding 11: Test Runs Manifest, But Manifest Need Not Include Negative Specs
**Q**: 5, 8  
**File:line**: `commands/vg/_shared/test/regression-security.md:96-114`, `commands/vg/test-spec.md:469-498`  
**Purpose**: test runs specs from `CODEGEN-MANIFEST.json`.  
**Actual**: manifest gate only checks at least one spec. No required edge/negative/failure families.  
**Gap**: manifest can contain only happy lifecycle specs.  
**Severity**: high  
**Fix**: manifest schema needs `spec_kind: happy|edge|negative|failure` and per-goal required counts.

### Finding 12: CrossAI Semantic Sweep Skippable
**Q**: 2, 3, 4  
**File:line**: `commands/vg/test-spec.md:242-262`, `commands/vg/test-spec.md:315-327`  
**Purpose**: catch semantic gaps.  
**Actual**: `--no-crossai-review` skips; low-stakes profile auto-skips.  
**Gap**: missing negative/edge/failure coverage can pass deterministic gates.  
**Severity**: medium  
**Fix**: make CrossAI mandatory when generated specs lack edge/negative/failure families.

| Stage | Verdict | Reason |
|---|---|---|
| review → test-spec | FAIL | required review outputs are not enforced or fully consumed |
| test-spec → test | PARTIAL | test runs manifest, but manifest lacks required edge/negative/failure coverage |

**Summary Table**

| Area | Main Gap | Severity |
|---|---|---|
| Review artifact consumption | runtime/map/matrix/scan dropped | critical |
| Edge cases | optional or upstream-only | critical |
| Negative paths | not first-class | critical |
| Failure modes | optional deep-probe | high |
| Status routing | READY variants mismatch | critical |
| Manifest integrity | runs what exists, not what should exist | high |

**Top 5 Recommendations**

1. Create canonical schemas for `GOAL-COVERAGE-MATRIX.json`, `LIFECYCLE-SPECS.json`, `CODEGEN-MANIFEST.json`.
2. Add mandatory `edge_specs[]`, `negative_specs[]`, `failure_specs[]` per goal.
3. Fix `verify-spec-stage-coverage.py` to consume `steps[].stage`.
4. Block test-spec on `NOT_SCANNED`, `BLOCKED`, missing review artifacts, stale matrix.
5. Require manifest coverage counts per goal: happy + edge + negative + failure.

**Edge-Case + Negative-Spec Coverage Verdict**

FAIL. Current deterministic test-spec mainly produces happy-path lifecycle/RCRURDR contracts. Edge/negative/failure tests exist only as optional prompt paths, upstream `EDGE-CASES`, or deep-probe variants, and are not guaranteed in `CODEGEN-MANIFEST.json`.